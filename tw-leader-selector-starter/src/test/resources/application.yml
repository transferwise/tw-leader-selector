spring:
  application:
    name: tw-leader-selector-starter-test

tw-curator:
  zookeeper-connect-string: "${ZOOKEEPER_CONTAINER_HOSTNAME}:${ZOOKEEPER_TCP_2181}"
  retry-times: 1
  session-timeout: 5s
