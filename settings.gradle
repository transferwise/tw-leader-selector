pluginManagement {
    repositories {
        maven {
            setUrl("https://arti.tw.ee/artifactory/plugins-release")
        }
        mavenLocal()
    }
}

rootProject.name = 'tw-leader-selector-root'

include 'testapp'
include 'tw-leader-selector'
include 'tw-leader-selector-starter'
